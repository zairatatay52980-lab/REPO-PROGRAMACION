package Vista;

public class MenuCuentasTitulares {

    /*
     * asignar titulares a cuentas :
     * (pedir la cuenta y despues lod dni, IBAN(ES[0-9]) de la cuenta
     * pasarlo al banco controler despues de verificar que existe el titular y la cuenta
     *"select id from cuetnas_corriente where iban = ?"
     * cuetna titular tiene los id de ambos
     * despues asignar los titulares
     * que introduzca el dni del titular
     * obtener el id mediante el dni
     * "select * from titular where dni = ?"
     * asignar el titular a la cuenta (idcuenta, idtitular)
     * "insert into cuetnastitulares (idcuenta, idtitular) values (?, ?)"
     *
     *
     *
     *
     *
     * */
}
